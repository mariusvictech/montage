# assets — video-4

Une seule pièce attendue : `rush.MOV`, la vidéo face caméra.
Pas de capture d'écran cette fois — le montage est entièrement en motion
design.

`sfx/` contient les sons, fabriqués par `scripts/sfx.mjs`. Les vidéos restent
en local : git les ignore, il faut les ajouter avec `git add -f`.
