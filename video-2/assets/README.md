# assets — video-2

Déposer ici :

- `rush.mp4` — la vidéo à monter ;
- les captures d'écran à illustrer (`checkout.png`, `boutique.png`, …).

`sfx/` contient les sons du montage, fabriqués par `scripts/sfx.mjs`.
Les vidéos et images restent en local : git les ignore (voir `.gitignore`).
